generator client {
  provider        = "prisma-client-js"
  output          = "../node_modules/prisma-n64-client"
  engineType      = "library"
  binaryTargets   = ["native", "rhel-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("N64_DATABASE_URL")
}


model Game {
  id          String   @id
  cover_path  String?
  created_at  DateTime @default(now())
  developer   String?
  publisher   String?
  release_jp  String?
  release_na  String?
  release_pal String?
  title       String
  cover_data  Bytes?
}
